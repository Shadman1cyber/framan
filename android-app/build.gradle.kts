// Root project needs no plugins: versions are managed in gradle/libs.versions.toml
// and each module declares exactly the plugins it applies (see below).
// NOTE: do NOT add org.jetbrains.kotlin.plugin.compose here — see android-app docs.

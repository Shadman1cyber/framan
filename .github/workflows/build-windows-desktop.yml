name: Build Windows desktop app

on:
  push:
    branches: [main, master]
    paths:
      - 'electron/**'
      - 'build/icon.*'
      - 'electron-builder.yml'
      - 'prisma/schema.prisma'
      - 'prisma/seed.ts'
      - 'scripts/build-desktop.cjs'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/build-windows-desktop.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Full pipeline: SQLite client + next build + seeded template DB +
      # cafe-id meta + NSIS (x64+ia32) + Portable installers. Windows runner
      # produces Windows Prisma engines — required for the package to work.
      - name: Build desktop installers
        run: node scripts/build-desktop.cjs
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
          NEXT_TELEMETRY_DISABLED: '1'

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cafe13-desktop-windows
          path: |
            dist-desktop/*.exe
          if-no-files-found: error
